# AGENTS.md

## 作業開始前の必須手順（最優先・例外なし）

1. エージェントは、調査、計画、コマンド実行、スキル利用、ファイル編集、コミット、プッシュを始める前に、必ずリポジトリ直下の `.\COMMON-AGENTS.md` を開き、先頭から末尾まで全文を読む。
2. `COMMON-AGENTS.md` はGit管理外のシンボリックリンクである。`git`や既定のignore設定が有効な`rg --files`の検索結果だけで、ファイルが存在しないと判断してはならない。PowerShellでは最初に次を実行する。

```powershell
Get-Content -Raw -LiteralPath .\COMMON-AGENTS.md
```

3. 読み取りに失敗した場合、出力が省略された場合、または末尾まで読めたことを確認できない場合は、一切の作業を開始せず、パスとシンボリックリンク先を確認して全文を再取得する。必要なら分割して末尾まで読む。
4. 全文を読了するまで、ローカル `AGENTS.md` だけを根拠に作業を続けてはならない。読了後は `COMMON-AGENTS.md` を最優先の指針とし、読了直後の最初の進捗報告で全文を読了したことを明示する。
   このファイルでは `NyTweetDeck` 固有の補足だけを記載する。

## 目的
- XのWeb APIを利用し、最終的には https://github.com/dimdenGD/OldTweetDeck のようなデザインの TweetDeck クローンを作り上げる
- Javaアプリで、専用ローカルアドレスにブラウザでアクセスすると専用フロントエンドが表示されるイメージを私は持っている
- 通信はX公式WebログインとWeb APIへ統一し、Android互換経路は保持しない

## 機能
- X (旧 Twitter) にログインする
- ログイン状態を維持する
- 複数アカウントのログイン状態を維持し、簡単に切り替えられる
- 保存済みアカウントがある場合は前回選択を優先し、利用不能または未選択なら一覧の#1を自動選択して保存済みカラムの初期化と表示まで開始する
- TweetDeckはビューポートの左端に縦長にアイコンを並べたメインメニューがある。
- メインメニューのアイコンはOldTweetDeckと同じ並び順。ポスト作成、検索、ホーム（おすすめ）、通知、ダイレクトメッセージ、トレンド
- このメインメニューの並びはデフォルトであり、「+」アイコンで任意に追加・削除可能にする
- デフォルトには並んでいないが追加できるアイコンは「フォローする」、「チャット」、「Grok」、「プレミアム」、「プロフィール」、「コミュニティ」、「クリエイタースタジオ」、「ビジネス」、「広告」、「スペースを作成」（これらの追加可能アイコンは Web版Xの基準であり、常に必要な機能ではない） 
- 「+」下部にはスペースを空けて複数アカウント切替アイコン、設定アイコンを並べる
- メインメニューの並びもドラッグ・アンド・ドロップで並び替え可能にする
- そのメインメニューの右側は追加していく「カラム」になっている（デフォルト状態では空）
- 追加するカラムのスペースの大きい「+」ボタンを押すとカラム追加メニューが表示され、追加できる。
- 「カラム」は「おすすめ」や「通知」、「履歴」（以前はブックマークやリストという名前だった）、「特定の指定したユーザー」や「リスト（アカウントに紐付いている作成したリストや他人が作ったリスト）」をポスト時系列順に並べる
- デフォルト設定では最新ポストがビューポート上部に来る順になっている
- 作成できる「カラム」の数は無制限である
- 設定したメインメニューの並びとカラムの並びは自動的に記憶し、アクセスしたときに再表示する
- メニュー、カラム、表示設定、検索履歴は認証情報と選択アカウントを含めずJSONでインポート・エクスポートできる
- カラム上部にタイトルと「-」ボタンを表示する
- マイナスボタンを押すとカラムを削除する
- 技術的に可能であれば、タイマーによる定期自動更新ではなく、レート制限を回避するため、新しくポストが投稿されたときだけ検知して自動更新する
- 各ポストにはユーザー名、ユーザーID、現在時刻からのポスト相対投稿時刻、Grokボタン、ハンバーガーメニュー（ハンバーガーメニューの内部には「このポストに興味がない」、「フォロー」、「リストから追加と削除」、「ミュート」、「ブロック」、「ポストアクティビティ」、「埋め込み」、「報告」、「コミュニティノートリクエスト」）、ポスト本文とハッシュタグ（あれば）、メディア（あれば）、リプライボタン、リツイートボタン（リツイートボタンの中身は「リポスト」「引用」）、いいねボタン、インプレッション数、ブックマークボタン、共有、メディアダウンロードボタンを表示する
- リポストはリポスト者を薄灰色の小さなアイコン付き文脈として左上にだけ表示し、リポスト元を通常ポストと同じカードとして表示する
- 引用ポストはWeb版X準拠の境界付き埋め込みカードとして表示し、通常ポストと同じ自動翻訳・原文切替を適用して、選択時はNyTweetDeck内の引用元詳細へ遷移する
- 各ポストをクリックすると詳細表示になり、ポストの詳細に入る前のボタンを維持したままリプライが表示される
- コミュニティノート通知を選択すると、Web版X準拠で対象ポスト本体と完全なノート本文・出典リンクをNyTweetDeck内の同じ詳細画面に表示する
- リプライボタンとリツイートボタン、いいねボタン、インプレッション数、ブックマークの各横にはリアルタイムの数字が表示される
- 各カラムは基本的に無限スクロール（ページネーション）でスクロールするほど古いポストが時系列で表示される
- 設定で言語を切り替えられる
- UIの表示言語とポストの言語が異なる場合はXがネイティブ提供する翻訳だけで既定の自動翻訳を行い、Google等の外部翻訳へフォールバックせず、投稿ごとの原文切替と各投稿の歯車から全カラム一括ON/OFFを可能にする
- 新着ポストのリアルタイム翻訳は表示範囲を優先し、Xのレート制限値と解除時刻を追跡して過剰な一括通信を避け、一時失敗時は自動再試行する。成功率と残り利用枠は設定画面で確認可能にする
- 設定でダークモードとライトモードとシステムプリファードを切り替えられる
- 動画は初期ミュートを維持しつつ既定でループ再生をONにし、設定でループ再生とミュート解除時の音量を変更して永続化できる
- その他設定で変更可能な項目は Web版 X 準拠とする


## 展開
- リリースワークフローを整備してリリースする
- 自動化検証する
- Windows Task Scheduler、macOS LaunchAgent、Linux systemd user serviceでログオン自動起動を登録・解除できる
- 端末ローカルのhostsと信頼済み証明書だけを用い、外部公開せず`https://ny.tweetdeck.com`からもloopback接続できる

## 認証情報の保存
- 自動起動・自動検証を人手なしで成立させるため、X WebセッションをOS標準のユーザー別アプリケーションデータ領域へ暗号化せず保存する方式を採用する。この平文保存はプロジェクト所有者がリスクを理解したうえで明示的に許可している。保存先はインストール場所や作業ディレクトリへ依存させない。
- 保存先をloopback上で動作するNyTweetDeckプロセスの信頼境界とし、秘密情報をGit、ログ、APIレスポンス、ブラウザの`localStorage`へ含めない。
- 保存処理ではスキーマ検証、原子的置換、直前バックアップ、破損時復旧を維持し、POSIX環境では所有者だけが読み書きできる権限を設定する。

## 環境
- フロントエンドのバンドルには Bun bundler を使用する
- Java LTS 17・21・25を正式対応とし、Java 17 bytecodeを生成する
- CIではactions/setup-javaが公式対応する非推奨でない全JDKディストリビューションを17・21・25で固定検証する
