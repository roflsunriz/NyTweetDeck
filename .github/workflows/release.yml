name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
          java-package: jdk
          check-latest: false
          cache: maven

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.0

      - name: Verify frontend
        working-directory: frontend
        run: |
          bun install --frozen-lockfile
          bun run lint
          bun run format:check
          bun run type-check
          bun run test
          bun audit

      - name: Validate release tag and source branch
        shell: bash
        env:
          RELEASE_VERSION: ${{ github.ref_name }}
        run: |
          version="${RELEASE_VERSION#v}"
          if [[ ! "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]; then
            echo "リリースタグはvから始まるSemVerで指定してください。" >&2
            exit 1
          fi
          git fetch origin main --depth=1
          git merge-base --is-ancestor "$GITHUB_SHA" origin/main
          echo "RELEASE_VERSION=$version" >> "$GITHUB_ENV"

      - name: Build application
        run: >-
          mvn --batch-mode --no-transfer-progress
          "-Drevision=${RELEASE_VERSION}" clean verify

      - name: Verify packaged browser UI
        run: bash ./scripts/verify-ui.sh "target/nytweetdeck-${RELEASE_VERSION}.jar"

      - name: Verify autostart installers
        shell: pwsh
        run: ./scripts/verify-autostart.ps1 "target/nytweetdeck-${RELEASE_VERSION}.jar"

      - name: Verify local HTTPS domain
        shell: pwsh
        run: ./scripts/verify-local-domain.ps1 "target/nytweetdeck-${RELEASE_VERSION}.jar"

      - name: Audit Maven dependencies
        shell: pwsh
        run: ./scripts/audit-maven.ps1

      - name: Package release
        shell: bash
        run: |
          mkdir -p dist/NyTweetDeck
          cp "target/nytweetdeck-${RELEASE_VERSION}.jar" dist/NyTweetDeck/NyTweetDeck.jar
          cp scripts/run-nytweetdeck.ps1 scripts/run-nytweetdeck.cmd scripts/run-nytweetdeck.sh dist/NyTweetDeck/
          cp scripts/install-autostart.ps1 scripts/uninstall-autostart.ps1 dist/NyTweetDeck/
          cp scripts/install-autostart.sh scripts/uninstall-autostart.sh dist/NyTweetDeck/
          cp scripts/install-local-domain.ps1 scripts/uninstall-local-domain.ps1 dist/NyTweetDeck/
          cp scripts/install-local-domain.sh scripts/uninstall-local-domain.sh dist/NyTweetDeck/
          cp README.md CHANGELOG.md how-to-update.md dist/NyTweetDeck/
          cp -r docs dist/NyTweetDeck/
          chmod +x dist/NyTweetDeck/run-nytweetdeck.sh dist/NyTweetDeck/install-autostart.sh dist/NyTweetDeck/uninstall-autostart.sh
          chmod +x dist/NyTweetDeck/install-local-domain.sh dist/NyTweetDeck/uninstall-local-domain.sh
          cd dist
          zip -r "NyTweetDeck-${GITHUB_REF_NAME}.zip" NyTweetDeck
          sha256sum "NyTweetDeck-${GITHUB_REF_NAME}.zip" > "NyTweetDeck-${GITHUB_REF_NAME}.zip.sha256"

      - name: Verify release archive
        shell: bash
        run: |
          archive="dist/NyTweetDeck-${GITHUB_REF_NAME}.zip"
          (cd dist && sha256sum -c "NyTweetDeck-${GITHUB_REF_NAME}.zip.sha256")
          for required in \
            NyTweetDeck/NyTweetDeck.jar \
            NyTweetDeck/run-nytweetdeck.ps1 \
            NyTweetDeck/run-nytweetdeck.cmd \
            NyTweetDeck/run-nytweetdeck.sh \
            NyTweetDeck/install-autostart.ps1 \
            NyTweetDeck/install-autostart.sh \
            NyTweetDeck/uninstall-autostart.ps1 \
            NyTweetDeck/uninstall-autostart.sh \
            NyTweetDeck/install-local-domain.ps1 \
            NyTweetDeck/install-local-domain.sh \
            NyTweetDeck/uninstall-local-domain.ps1 \
            NyTweetDeck/uninstall-local-domain.sh \
            NyTweetDeck/README.md \
            NyTweetDeck/CHANGELOG.md; do
            unzip -Z1 "$archive" | grep -Fx "$required"
          done
          unzip -p "$archive" NyTweetDeck/NyTweetDeck.jar > target/release-archive.jar
          cmp "target/nytweetdeck-${RELEASE_VERSION}.jar" target/release-archive.jar

      - name: Publish GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: >-
          gh release create "$GITHUB_REF_NAME"
          "dist/NyTweetDeck-${GITHUB_REF_NAME}.zip"
          "dist/NyTweetDeck-${GITHUB_REF_NAME}.zip.sha256"
          --verify-tag
          --generate-notes
