# 変更履歴

このプロジェクトに対する注目すべき変更は、このファイルに記録します。

書式は[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)に基づきます。

## [Unreleased]

### Added

- Android設定からGitHub Releases APIで最新の安定版`android-v*`リリースを確認し、署名済みAPKを端末のダウンロードフォルダーへ直接保存する導線を追加した。

### Changed

- デスクトップ版の動画を再生範囲へ入る前から近傍で先読みし、自動画質で読み込みが停止した場合は再生位置を保って低いビットレートへ切り替えるよう変更した。
- デスクトップ版とAndroid版のポスト詳細で、タイムライン等に取得済みの対象ポストを通信完了前から表示し、Android版は対象ポストの重複取得も省略するよう変更した。
- Androidの複数画像を切替前から画面外へ横並びにし、隣接画像との境界が画面中央を越えたときだけ表示画像を確定するよう変更した。
- デスクトップ版とAndroid版でユーザーのミュートまたはブロックに成功した際、そのユーザーが投稿・リポストしたポストを表示中の全タイムラインから除外するよう変更した。

### Fixed

- デスクトップ版とAndroid版で投稿本文の`&lt;`・`&gt;`などが文字参照のまま表示されないよう、HTML文字参照をURL正規化前に一度だけ安全にデコードするよう修正した。
- デスクトップ版でピンチ操作用のポインターキャプチャが動画コントローラの入力を奪っていた問題を解消し、再生・シーク・ミュート・音量・ループ・全画面・Picture-in-Pictureを操作できるようにした。
- デスクトップ版の音量スライダーをドラッグした際に、動画要素のネイティブドラッグへ移行してサムネイルのゴーストが表示されないよう修正した。
- ポスト詳細の動画操作が親カラムの並べ替えを開始しないよう、カラムのドラッグ開始領域をヘッダー内の専用ハンドルだけへ限定した。
- デスクトップ版とAndroid版でシークまたは音量を直接操作した際、初期ミュートが自動解除されるよう修正した。

## [Android 0.2.1] - 2026-09-02

### Fixed

- 長いプロフィール詳細が画面を占有してポスト・メディア一覧へ到達できない問題を解消するため、プロフィール情報、タブ、一覧を同じ縦スクロール領域へ統合した。

## [1.4.0] - 2026-09-02

### Added

- Issue #1に対応し、デスクトップ版でタイムラインの自動更新を停止・再開できる設定、カラムごとの「トップ／最新」投稿順、表示言語とは独立した投稿の翻訳先言語、動画画質の既定値と再生中の画質選択を追加した。
- 返信ポストを開いたときに取得できた親返信を対象ポストの前へ表示し、詳細画面の末尾から過去の返信を追加で読み込めるようにした。
- カラムの表示領域を広げながらメニューへすぐ戻れるよう、メインナビゲーションを常時表示または自動収納へ切り替え、画面端へのマウスホバーで一時表示できるようにした。

### Changed

- Xが返す複数のMP4画質を保持し、検索カラムの「トップ」はXの検索ランキングを利用し、その他のタイムラインでも取得済み投稿をエンゲージメント順で確認できるようにした。
- 再生中の映像を隠さず確認できるよう、動画コントローラを操作後3秒で自動的に隠し、ポインター操作またはキーボードフォーカスで再表示するよう変更した。

### Fixed

- Issue #1の設定拡張後も回帰検証を正しく継続できるよう、Web・Javaのテスト期待値を現行レイアウト版へ更新した。
- リリース環境の描画差で遅延動画の読み込みタイミングに影響されないテキスト投稿を基準にし、スクロール移動が描画へ反映された後の位置を基準にブラウザ検証するよう修正した。

## [Android 0.2.0] - 2026-09-02

### Added

- Issue #1に対応し、Android版でタイムラインの自動更新を停止・再開できる設定、カラムごとの「トップ／最新」投稿順、表示言語とは独立した投稿の翻訳先言語、動画画質の既定値と再生中の画質選択を追加した。
- 返信ポストを開いたときに取得できた親返信を対象ポストの前へ表示し、詳細画面の末尾から過去の返信を追加で読み込めるようにした。
- カラムの表示領域を広げながらメニューへすぐ戻れるよう、メインナビゲーションを常時表示または3秒後の自動収納へ切り替え、配置に応じた画面端からのスワイプで一時表示できるようにした。
- 狭い画面で横幅を最大限使えるよう、メインナビゲーションをサイドまたは画面下部へ切り替える設定を追加した。

### Changed

- Xが返す複数のMP4画質を保持し、検索カラムの「トップ」はXの検索ランキングを利用し、その他のタイムラインでも取得済み投稿をエンゲージメント順で確認できるようにした。
- 動画のピンチ拡大・縮小を1〜4倍の同じ変換経路へ集約し、指の中心を保った拡大、拡大時のパン、縮小時の位置リセットを行うよう変更した。

### Fixed

- GitHub ActionsのNode.js 20廃止後もAndroid成果物を公開できるよう、公式`actions/upload-artifact`をNode.js 24対応のv6へ更新した。
- Issue #1の設定拡張後も回帰検証を正しく継続できるよう、Androidの設定schemaテストを現行版へ更新し、主要10言語で不足していたメインナビゲーション設定文言を追加した。
- Androidの全検証が適応更新テストの終了待ちで停止したり、端末専用ログAPIの未mock呼び出しで失敗したりしないよう、テスト終了時にViewModelを実ライフサイクルと同じ経路で破棄し、メタデータ更新失敗ログを注入可能な境界へ分離した。
- AndroidでX Web定義更新がWebView専用User-Agentにより拒否されないよう、端末のブラウザ版情報を保ったままWebView識別子だけを除去して公式公開資産を取得するよう修正した。
- Android設定の機能抜けを防ぐため、表示だけで未接続だった投稿の翻訳先言語を永続設定へ接続し、フィード自動更新を設定上部の独立セクションへ移して停止時も継続する処理を明記した。

## [Android 0.1.3] - 2026-09-01

### Changed

- 画像と動画を端末のシステムバーまで覆う全画面で確認できるようにし、動画詳細でもインラインと同じ再生・シーク・ミュート・音量・ループ操作を使用できるよう変更した。
- 動画を横幅いっぱいに拡大して視聴できるよう、全画面表示を端末回転後も維持し、自動回転に加えて横向きへ切り替える操作を追加した。
- 映像を隠さず確認できるよう、インラインと全画面の動画コントローラを操作後3秒で自動的に隠し、映像のタップで再表示するよう変更した。

## [Android 0.1.2] - 2026-09-01

### Added

- X Webで観測した12件の意味契約をAndroidにも適用するため、非modalのポスト／リポストメニュー、カラムを保持する詳細・プロフィール・composer・画像・アカウント経路、Back履歴、画面外でplayerを破棄・再生成する独自動画操作UIを追加した。

### Fixed

- ユーザープロフィールの返信タブが廃止済みGraphQL operationの404で読み込めない問題を解消するため、公式Webが現在使用する`UserRepliesTimeline`へWeb・Android共通定義を更新した。

## [1.3.2] - 2026-09-01

### Added

- X Webの動画操作と資源解放を独自実装へ取り込むため、画面外でvideo要素を初期化・破棄して再入場時にミュート状態で再生成し、再生、一時停止、seek、mute、音量、loop、Picture-in-Picture、fullscreenを提供する多言語custom playerを追加した。

- X公式Webをコードコピーではなく挙動リファレンスとして独自実装へ反映できるよう、ログイン済み専用Chromeから許可済み公式JS/CSSだけをGit管理外へ採取し、Cookie・header・HTML・DOM・実ユーザーデータを保存せずメモリー上でde-minifyする研究sandboxを追加した。
- 動画、画像ビューア、返信ツリー、URL正規化、プロフィール遷移の合成mockを安全に公式moduleへ与える前提を固定するため、CookieとWeb Storageを持たない新規BrowserContextでloopback以外のHTTP・HTTPS・WebSocket・FTPを遮断し、mock搬送を実測する自動検証を追加した。
- URL正規化の独自実装へ観測事実を反映できるよう、公式資産からURL entity補完関数だけをメモリー抽出し、外部通信を全面遮断した環境で欠落・片側欠落・空文字を含む合成5ケースを実行して意味契約だけを記録するrunnerを追加した。
- 代表的な5領域だけでX互換を判断しないよう、全surface・コンテンツ型・静的／動的要素・状態・入力・環境・データ条件のカバレッジ台帳と、公式bundle token・role・タグ・安全な固定test ID・ARIA状態を匿名で自動発見するinventoryを追加した。
- 初期ホームだけをX全体の代表にしないよう、変更操作を含まない12 surface巡回と、タブ・menu・作成・詳細・プロフィール・画像viewerを3 viewportで比較する動的scenario matrixを追加し、識別子を保存せずroute・menu・modal・ARIA状態を契約化した。
- X動画playerとの差を推測で埋めないよう、公開動画検索から中央表示・画面外・再表示時の再生、ミュート、loop、native controls、source、readyState、DOM接続を匿名観測するrunnerを追加した。

### Changed

- X Web版の意味契約を全面的に取り込んでもTweetDeckとしての利用体験を失わないよう、自由追加・並べ替え可能な任意数カラム、カスタマイズ可能な左メインメニュー、複数アカウント保持・切替を製品不変条件としてカバレッジ台帳と検証手順へ固定した。
- Xと同じ非modal menu状態を提供するため、デスクトップ版のポストoverflowとリポストを実button・`role=menu`・`menuitem`・`aria-expanded`へ統一し、全viewportで観測したEscape閉鎖を追加した。
- Xの狭幅viewerを常時dialogとして扱わないよう、デスクトップ・タブレットではmodal dialog、電話幅では非modalの全画面regionへ切り替えるレスポンシブ意味契約をWeb版へ反映した。
- Xの作成画面と同じviewport分岐にするため、Web版Composerをデスクトップ・タブレットではmodal dialog、電話幅では余白・枠・背景blurを持たない非modal全画面regionとして表示するよう変更した。
- 正規化済みURLを本文上で操作できるよう、HTTP(S) URLを末尾句読点・不釣り合いな閉じ括弧から分離して安全なリンクにし、クリック時にポスト詳細を誤って開かない本文セグメントrendererをWeb版へ追加した。
- Androidでも同じURL意味契約を使えるよう、HTTP(S) URL・ハッシュタグ・通常文字列を分離する純粋parserとCompose `LinkAnnotation.Url`描画を追加した。
- Xと同じ表示名と遷移先を使い分けるため、Java・TypeScript・Kotlinの通常ポストと引用へTextLinkモデルを追加し、`display_url`を表示しながら`unwound_url`優先の安全な実URLへ遷移するよう変更した。
- Xと同じBack操作でoverlayから戻れるよう、Composer、画像viewer、ポスト詳細、プロフィールをhash route履歴へ接続し、閉じる操作とブラウザーBackで背後のカラムへ復帰する共通hookを追加した。
- Xの詳細surfaceをmodalとして読み上げないよう、Web版のポスト詳細とプロフィールを`aria-modal`を持たないroute regionへ変更した。
- Xのviewport別アカウント導線に合わせ、Web版アカウント切替を広幅では左メニュー近傍の非modal group、電話幅では非modal dialogとして表示するよう変更した。

### Fixed

- ユーザープロフィールの返信タブが廃止済みGraphQL operationの404で読み込めない問題を解消するため、公式Webが現在使用する`UserRepliesTimeline`へWeb・Android共通定義を更新した。

## [Android 0.1.1] - 2026-08-30

### Added

- 長い会話でも返信同士の関係を追えるよう、`replyToPostId`から安全に階層を求め、Bに付いたE、Eに付いたFを縦線・枝線・有限のインデントで結ぶ表示を追加した。親欠損、自己参照、循環、過深度でも応答順を変えず表示を継続する。
- リストカラムを待たずに選べるよう、アカウント別に自分のリスト・おすすめ・検索結果を先読みして保持し、キャッシュを即時表示してからバックグラウンド再検証する専用リスト選択ダイアログを追加した。

### Changed

- 動画の再生開始、再入場、詳細表示を滑らかにするため、インラインと詳細の再生基盤をMedia3へ統一し、256MiBの共有ディスクキャッシュ、先読みバッファ、可視範囲に連動した再生、ループ・音量・ミュートを同じプレイヤーへ接続した。
- Androidの任意形状マスクとテーマアイコンへ自然に適応するよう、ランチャー画像を背景・前景・単色レイヤーに分離したadaptive iconへ変更した。
- 過去位置を読んでいる間も新着を見落とさないよう、新規投稿通知をカラム上部の独立表示へ移し、押して先頭へ移動するまで件数と投稿者を保持するよう変更した。
- 返信操作を一目で識別できるよう、折れ矢印を輪郭付き吹き出しアイコンへ変更した。
- 同一ポストの複数画像を続けて確認できるよう、画像詳細を左境界へ動かすと次、右境界へ動かすと前の画像へ循環移動し、ピンチ・パン中は1ジェスチャーにつき1回だけ切り替えるよう変更した。

### Fixed

- 自動再生OFFでも動画詳細の再生ボタンを使用でき、準備完了前の操作も再生へ引き継がれるよう修正した。インライン動画を開いたまま詳細プレイヤーが二重再生される問題も防止した。
- ポスト詳細の追加読込が同じ返信カーソルや循環カーソルで無限に続かないよう、取得済みカーソルを追跡して終端化し、ページ追加時の不要な対象ポスト再取得も省いた。
- 画像・動画・X記事以外の`https://t.co/...`を短縮表示のまま残さないよう、通常ポスト、長文ポスト、引用、事前翻訳で`unwound_url`、次に`expanded_url`を使って安全なHTTP(S)のオリジナルURLへ正規化した。

## [1.3.1] - 2026-08-30

### Added

- 長い会話の親子関係を把握できるよう、返信の取得・並び替え順を維持したまま縦線・枝線・有限のインデントでB-E-Fのような返信経路を表示し、LTR/RTLと狭幅画面へ対応した。
- READMEの画面を再現可能に更新できるよう、実アカウントへ接続しない固定フィクスチャと隔離Chromeからカバー、カラム、画像ビューア、設定画面を自動キャプチャするスクリプトを追加した。

### Changed

- 同一ポストの複数画像を続けて確認できるよう、画像詳細で左境界へドラッグすると次、右境界へドラッグすると前の画像へ循環移動し、前後ボタンと左右キーでも切り替えられるよう変更した。
- 左メニューを機能だけに整理するため、操作できない青いNを削除し、ポスト作成をAndroid版と同じ鉛筆アイコンへ統一した。

### Fixed

- 画像詳細がポインター解放後も移動したり、ズームとドラッグの競合で移動状態が残ったりしないよう、押下・移動・解放・取消・キャプチャ喪失を明示的な状態遷移として修正した。
- 画像・動画・X記事以外の`https://t.co/...`を短縮表示のまま残さないよう、通常ポスト、長文ポスト、引用、事前翻訳で`unwound_url`、次に`expanded_url`を使って安全なHTTP(S)のオリジナルURLへ正規化した。

## [Android 0.1.0] - 2026-08-30

### Added

- NyTweetDeckの利用体験をスマートフォンで維持できるよう、X公式Webログイン、複数アカウント、無制限カラム、各種タイムライン、通知、トレンド、DM、ポスト詳細、投稿・返信・引用、メディア、翻訳、Live Pipeline、11言語、テーマ・表示設定を備えたネイティブAndroidアプリを追加した。
- 認証済み状態を失わず配布前の品質を確認できるよう、AQUOS実機で読み取り機能、可逆ポスト／ユーザー操作、複数アカウント切替、release更新を検証する自動テストと署名付きAPKのReleaseワークフローを追加した。

### Changed

- カラムを横移動した際に通信待ちや表示位置の変化を起こさないよう、アカウント別の取得済みデータとページ位置を即時復元し、表示後に遅れて更新して、新着通知と正確なスクロール位置を維持するNyTweetDeck方式へ統一した。

### Fixed

- Xの現行Web APIでリスト会員追加・削除を実行できるよう、GraphQL変数を現行のcamelCase形式へ更新した。
- 実機検証や更新でログイン状態を消去しないよう、本体APKへtest-onlyオプションを適用せず、同一署名を確認して上書きし、検証終了時は非debuggableなrelease版へ戻すよう修正した。
- Live Pipelineの停止時に割り込み例外でアプリプロセスを終了させないよう、接続・再接続・購読更新の中断を正常終了として処理するよう修正した。
- 配布前のブラウザ検証が起動済みNyTweetDeckへ接続して利用者設定を変更しないよう、HTTP/CDPへ空きポートを割り当て、専用プロセスの生存と一時設定領域を確認するよう修正した。

## [1.3.0] - 2026-08-29

### Added

- フォロー通知に含まれる利用者を確認できるよう、通知カードからフォローしたユーザー一覧を開き、識別できた利用者はNyTweetDeck内のプロフィールへ移動できる導線を追加した。

### Changed

- メインメニューと第1カラムの境界を見分けやすくしながら横方向のリズムを揃えるため、その間隔を第1・第2カラム間と同じ可変幅へ統一した。
- 反応やメニュー操作を通信待ちで妨げないよう、いいね・リポスト・履歴保存は連続操作を即時反映して最終意図だけへ順序どおり収束させ、フォロー・ミュート・ブロック・リスト操作は押下時に完了表示して互いに独立して送信するよう変更した。
- 保存済みポストを未保存状態と一目で区別できるよう、ブックマークアイコンをいいね済みと同様に塗りつぶして表示するよう変更した。

### Fixed

- 更新や複数画面の同時操作でカラム設定が初期化・巻き戻りしないよう、共有設定の全旧版をカラムを保持したまま現行形式へ原子的に移行し、保存競合時は未保存の操作を最新リビジョンへ再適用するよう修正した。

## [1.2.1] - 2026-08-28

### Fixed

- 画像を表示範囲に合わせて小さく確認できるよう、フルサイズ画像の最小倍率を10%へ下げ、左ボタンを押しているドラッグ中だけ画像を移動して、ボタン解放を取りこぼしても移動状態を残さないよう修正した。
- タイムライン下部から上へ戻る際に表示位置が飛ばないよう、遅延コンテンツの高さ変化にはブラウザ標準のスクロールアンカーを利用し、新着差し込み時の手動補正は補正済みの現在位置を基準に適用するよう修正した。

## [1.2.0] - 2026-08-28

### Added

- ポスト詳細の返信をXネイティブの関連度・最新・いいね順で切り替えて選択を再表示時にも維持できるようにし、Xが`LowQuality`または`AbusiveQuality`と分類したスパムの可能性がある返信を既定で折り畳む表示を追加した。

## [1.1.2] - 2026-08-28

### Fixed

- Xの長文ポストがリポスト元や引用リポスト内で途中までしか表示されない問題を防ぐため、短縮された互換本文より`note_tweet`の完全な本文を優先して表示するよう修正した。

## [1.1.1] - 2026-08-28

### Fixed

- 自動更新中も読んでいるポストを見失わないよう、新着ポストを画面外の上部へ追加して表示位置を固定し、最大5アカウントの丸アイコンを重ねた新規投稿通知から先頭へ移動できるよう修正した。

## [1.1.0] - 2026-08-28

### Added

- 必要な投稿だけを一度に確認できるよう、タイムラインの文字・画像・動画フィルターを複数選択可能にし、その組み合わせへ独立して適用できる「リポストを除く」オプションを追加した。
- 最新状態を利用者の意思ですぐ確認できるよう、投稿・通知タイムラインの最上部からさらにスクロールすると、1ジェスチャーにつき1回手動更新する操作を追加した。
- 開発時にバージョン付きJAR名や複数ターミナルの手動操作へ依存せず起動できるよう、MavenバックエンドとBunホットリロード環境をまとめて開始し、準備完了確認・ブラウザ表示・一括停止を行うWindows/macOS/Linux用開発ランチャーを追加した。
- 画像を細部まで確認できるよう、ポスト詳細の画像をクリックすると原寸データを全画面表示し、ドラッグ移動、スクロール拡大・縮小、倍率リセット、Escによるポスト詳細への復帰を行える画像ビューアを追加した。
- Xの記事を外部画面へ移動せず読めるよう、記事付きポストにヘッダー画像・タイトル・冒頭概要を展開し、選択時にタイトル・ヘッダー・全文を表示する記事リーダーを追加した。

### Changed

- タイムラインの表示領域を広く保てるよう、すべて・文字・画像・動画・リポスト除外のフィルターを、読み上げ名とホバーツールチップを維持した1行のアイコンボタンへ変更した。
- タイムラインの再表示で通信完了まで空表示へ戻らないよう、アカウント・カラム種別・対象・表示言語ごとに取得済み投稿とページ位置をメモリへ保持し、最大200件を即時復元してからバックグラウンドで再検証するよう変更した。同一応答では表示中DOMと追加読込済みページを維持し、実差分がある場合だけ遅れて置き換える。
- リストカラムの候補画面で通信待ちを発生させないため、保存済みアカウントの自分・おすすめリストをアプリ起動時にアカウント別で先読みして保持し、画面復帰時のバックグラウンド確認で候補に実差分がある場合だけ表示を増減するよう変更した。同期失敗時は取得済み候補を維持する。

### Fixed

- 200件を超えて無限スクロールしたタイムラインをカラム再表示後も飛ばさず読めるよう、キャッシュへ完全に収まるページだけ投稿列と`nextCursor`を同時に進め、上限をまたぐページは再取得可能なカーソル位置を維持するよう修正した。キャッシュの即時表示とバックグラウンド再検証は従来どおり維持する。
- 共有設定ファイルの欠落や初回保存後の破損で起動不能にならないよう、`settings.json`がなくても有効な`settings.json.bak`を原子的に復元し、両方とも利用不能な場合は起動を継続して初期化経路から設定を自動再生成するよう修正した。初回保存時から復旧用コピーを作成し、破損した本体で正常なバックアップを上書きしないようにした。バックエンドの再起動中に設定読込が失敗した画面も、段階的な自動再試行で操作なしに復帰する。
- いいね・リポストで通信完了まで色と件数が変わらず待たされる感覚をなくすため、押下直後に表示へ反映してMutationをバックグラウンド実行し、失敗時だけ元の状態へ戻して理由を表示するよう修正した。あわせて、別種のポスト操作を一括で待機させないよう通信中状態を操作ごとに分離した。
- 開くアドレスによって表示設定・メニュー・カラム・検索履歴・選択アカウントが分断されないよう、オリジン別`localStorage`を設定の正本にする方式を廃止し、OSのユーザー別領域にある原子的・バックアップ付き共有設定へ移行した。`ny.tweetdeck.com`、`127.0.0.1`、`localhost`などから同じリビジョンを使用し、別画面の変更もSSEで同期する。初回だけ既存設定を安全に移行し、競合時は先に確定した共有設定を維持する。
- 画像・動画・X記事を示す`https://t.co/…`がポスト本文にも重複表示されないよう、Xのメディア／記事エンティティに一致するリダイレクトURLを原文と事前翻訳から省略するよう修正した。
- タイムラインの自動更新が接続直後に停止する問題を解消するため、X Webの現行`/live_pipeline/events`、先頭20 topicの自動購読、`/system/config`セッション確立、残りtopicの追加購読とTTL更新へ追従した。Live Pipelineで提供されない新規投稿は、表示中タブだけを対象に全カラムで15秒以上ずらし、変化がなければ60秒から最大5分まで間隔を延ばす低頻度更新で検知するようにした。
- 数字の差分反映ごとにLive Pipeline購読のDELETEとPUTが競合して購読自体が消える問題を防ぐため、投稿ID集合が変わった場合だけ購読を更新し、解除はカラム終了時だけ送るよう修正した。
- ローカルHTTPS設定の一部欠落が隠れたHTTP起動として見逃されないよう、Windowsランチャーで専用CAを検証付き自己修復し、修復不能時は永続化した理由付きエラーとして起動を停止するよう変更した。
- 更新後も自動起動タスクが古いJARや移動済み配布フォルダーを参照し続けないよう、JARとランチャーをユーザー別アプリ領域へ原子的に配置し、証明書・hosts・タスク更新・既存プロセス再起動・HTTP/HTTPS確認を一括実行する統合インストーラーを追加した。
- キーストアパスワードがプロセス一覧へ露出しないよう、WindowsとmacOS/LinuxのランチャーからJavaへ環境変数で渡すよう変更した。
- macOS/LinuxでもCAファイルの存在だけでHTTPSを有効にしないよう、起動前にOS信頼ストアでサーバー証明書とホスト名を検証するよう変更した。

## [1.0.1] - 2026-08-27

### Fixed

- `https://ny.tweetdeck.com`でブラウザの証明書警告が出ないよう、CAではない自己署名サーバー証明書を直接信頼する方式を廃止し、端末内の専用ルートCAで署名した証明書チェーンへ変更した。Windowsでは別管理者の証明書ストアへ誤登録しないよう、利用者権限のCA登録と管理者権限のhosts更新も分離した。
- WindowsとLinuxのどちらでも同じ証明書チェーン検証を実行できるよう、OpenSSL実行パスの解決をOS非依存に修正した。
- 高負荷のCI環境でもChromeの実ブラウザ検証を安定して開始できるよう、デバッグ接続の準備を実測しながら待つ上限を30秒へ延長した。

### Changed

- 多数の動画があるカラムで不要な通信と同時再生を避けるため、動画の自動ロードと自動再生をビューポート上端〜中央の再生範囲へ入った場合だけ行い、範囲を離れたら停止してメディア接続も解除するよう変更した。
- GitHub Release本文を自動生成のコミット一覧ではなく、対象バージョンのCHANGELOGセクションから生成するよう変更した。
- READMEと設計・監査資料から、初回公開前の保存形式、内部レイアウト移行、未完了状態に関する記述を削除した。

## [1.0.0] - 2026-08-27

### Added

- 別端末や別ブラウザへ画面構成を安全に移せるよう、認証情報と選択アカウントを除外したバージョン付きJSONで、メニュー・カラム・表示設定・検索履歴をインポート／エクスポートする設定導線と自動検証を追加した。
- OSログオン後に操作なしで利用開始できるよう、Windows Task Scheduler、macOS LaunchAgent、Linux systemd user serviceの登録・解除スクリプトとdry-run検証を追加した。
- 公開ネットワークへ露出せず覚えやすいURLを使えるよう、端末内のhosts・ローカル証明書・loopback限定HTTPSを設定／解除するWindows・macOS・Linuxスクリプトと、`https://ny.tweetdeck.com`と既存HTTPを併存させるコネクタを追加した。
- JDKメーカー固有の差を継続検出できるように、Temurin、Zulu、Liberica、Microsoft、Corretto、Semeru、Oracle、Dragonwell、SapMachine、GraalVM、GraalVM Community、JetBrains Runtime、Tencent KonaをJava 17・21・25で検証する固定CI行列を追加した。
- JavaアプリからローカルWeb UIを利用できる最初の製品経路を成立させるため、Spring BootサーバーとBun bundler製Reactフロントエンドを追加した。
- OldTweetDeck型UIの基礎として、左メニュー、空のデッキ、カラム追加・削除、横スクロール可能な無制限カラム表示を追加した。
- アクセス時に作業状態を復元できるよう、バージョン検証と破損データ回復を備えたレイアウト保存を追加した。
- 表示設定の基盤として、日本語・英語およびシステム・ライト・ダークテーマの切り替えを追加した。
- 退行を自動検出できるよう、フロントエンドの操作テスト、Java統合テスト、GitHub Actionsの検証ワークフローを追加した。
- 依存関係の既知脆弱性を認証情報なしで検出できるよう、Bun監査とOSVによるMaven依存監査を検証手順へ追加した。
- 複数アカウントのWebセッションを再起動後も操作なしで利用できるよう、バージョン付きアカウントストア、原子的な上書き、直前バックアップ、自動復旧を追加した。
- 実データをカラムへ表示できるよう、Web Cookie認証付きGraphQLクライアント、URTポスト・作者・メディア・数字・cursor正規化、ホーム・ユーザー・リスト・履歴・検索APIを追加した。
- 複数アカウントと対象付きカラムを保持できるよう、アカウント切替、対象付きユーザー・リストカラム、追加ページ読込をレイアウト保存へ追加した。
- ポスト操作を実データへ反映できるよう、いいね、解除、リポスト、取り消し、履歴保存、削除のGraphQL Mutationを追加した。
- 残る主要導線を実データへ接続するため、Web認証付き通知REST、ポスト詳細・返信一覧、投稿・返信Composer、メニュー・カラムのドラッグ並べ替えを追加した。
- 検証済み成果物を再現可能に配布するため、Windows/macOS/Linuxランチャー、ZIP、SHA-256、タグ連動GitHub Releaseワークフローを追加した。
- メインメニューを任意項目から編集できるようにし、11言語の選択、英語フォールバック、アラビア語・ウルドゥー語のRTL表示を追加した。
- 既定メニューの未接続導線を解消するため、Web認証付きDM受信箱RESTとGraphQL Exploreをメッセージ・トレンドカラムへ接続した。
- 定期ポーリングを避けるため、投稿・返信・ポスト操作の成功をアカウント別SSEでブラウザへ通知するイベント駆動更新を追加した。
- パスワードや2FA値をローカルアプリへ渡さずログインできるよう、X公式画面を分離Chromeで開き、完了後のHttpOnly Webセッションだけをローカルアカウントストアへ自動保存する複数アカウントログイン経路を追加した。
- X Web更新のたびに手作業で追従する必要をなくすため、公式Web資産からGraphQLのqueryId・operation metadata・Feature Switch値を定期取得し、全必須定義の検証成功時だけ原子的に反映する自動更新機構を追加した。
- 長いカラムから目的の投稿形式だけを確認できるよう、投稿を「すべて・文字・画像・動画」で追加通信なしに絞り込むフィルターを追加した。
- 作者を外部Webで開かずNyTweetDeck内で確認できるよう、ヘッダー・アイコン・名前・ID・説明・参加日・場所・Webサイト・フォロー数・フォロワー数・共通フォロワーを表示するプロフィール画面を追加した。
- プロフィール内で投稿を用途別に閲覧できるよう、すべて・ポスト・ハイライト・返信・メディアの各Webタイムラインと、メディア内の画像・動画フィルターを追加した。
- 各ポストから直接返信できるComposer、Grok導線、「興味がない」・フォロー・リスト・ミュート・ブロック・アクティビティ・埋め込み・報告・コミュニティノートを含むメニューを追加した。
- 手動操作に依存せず古いポストを連続表示できるよう、カラム末尾の接近を検知する無限スクロールを追加し、X Webで使われる`attachment_url`を用いた引用Composerを追加した。
- 主要言語の選択が英語表示へ戻らないよう、中国語・ヒンディー語・スペイン語・フランス語・アラビア語・ポルトガル語・ベンガル語・ロシア語・ウルドゥー語の実辞書を追加した。
- Web版Xに沿う表示調整を利用者が保存できるよう、文字サイズ、アクセント色、表示密度、動きの抑制、メディアプレビュー、動画自動再生を追加した。
- 定期ポーリングなしで数字とDMを更新できるよう、Web認証付きLive Pipeline SSE、表示中ポストとDMのtopic統合、切断時の指数バックオフ再接続、ローカルSSE転送を追加した。
- ポストメニューを外部プロフィールへの代替リンクで終わらせないため、フォロー・ミュート・ブロックのWeb認証付きRESTと、リストIDを指定した追加・削除GraphQL Mutationを接続した。
- 左メニューを無反応にしないため、検索を対象入力付きカラム、フォロー中を最新ホーム、プロフィールを選択アカウントのユーザーカラムへ接続し、任意項目へ確認済みX公式Web導線を追加した。
- リストIDを覚えて入力する必要をなくすため、アカウントに紐付くリスト、おすすめリスト、キーワード検索結果を名称・所有者付きで選べるカラム追加画面を追加した。
- 数値識別子の入力をなくすため、@ユーザー名を現行Web APIで解決して名称付きユーザーカラムを作成する導線を追加した。
- ユーザー向けの「ユーザーID」をXの@ユーザー名へ統一し、内部通信専用の数値識別子をポストとプロフィールから非表示にした。
- 配布前の画面退行を自動検出するため、パッケージ済みJARを起動して複数ビューポート・設定・RTLを操作するChrome検証をCIとReleaseの必須ゲートへ追加した。
- Exploreと通知の非ポスト要素を捨てないため、Web URTの`TimelineTrend`と`TimelineNotification`を専用正規化し、トレンド名・順位・説明およびフォロー等の通知イベントをポストと併せて表示するカラムを追加した。
- 表示言語が異なるポストを読みやすくするため、X公式Webの事前生成済み翻訳を優先し、未生成の新着だけX翻訳APIで補う既定ONの自動翻訳、投稿ごとの原文／翻訳切替、歯車から全カラムへ反映するON/OFF設定、失敗時の再試行を追加した。
- 多数のトレンドから関心のある話題を探せるよう、名称・説明・分類を検索ワードで即時絞り込みできる入力欄と、確定した直近20件を候補表示する検索履歴を追加した。検索ワードと履歴は自動保存し、再起動後も復元する。
- コミュニティノートの根拠を外部Webへ移動せず読めるよう、X Webと同じ`BirdwatchFetchOneNote`から完全なノート本文・出典・対象ポストIDを取得し、`TweetResultByRestId`の通常ポストカードと同じ詳細画面へ統合する経路を追加した。
- 動画を繰り返し確認しやすくするため既定ONのループ再生設定と0〜100%の音量設定を追加し、初期ミュートを維持したまま全ポストの動画へ即時反映して永続化するようにした。

### Fixed

- 引用元だけ原文のまま残る不整合をなくすため、引用ポスト内でもXの事前翻訳を優先し、未生成なら通常ポストと共通の表示範囲・レート制御付きリアルタイム翻訳を適用して、引用内で原文／翻訳を切り替えられるよう修正した。
- 新着ポストのXリアルタイム翻訳がカラム内の画面外ポストまで一括要求して利用枠を消費していたため、表示付近だけを最大2並列で翻訳し、429・一時的なサーバー障害・タイムアウトを`Retry-After`または段階的待機後に初回を含め最大4回試行するよう修正した。X応答の利用枠・リセット時刻と成功率を追跡し、残り5%は競合回避用に確保して設定画面へ稼働状況を表示する。
- 起動のたびにアカウントを手動選択せず保存済みカラムを表示できるよう、前回選択した保存済みアカウントを優先し、利用不能なら保存一覧の#1へ自動フォールバックして初期読込を開始するよう修正した。
- いいね等でカラムの位置や内容が突然入れ替わらないよう、いいね・リポストの成功イベントによるタイムライン全体の再取得を廃止し、Live Pipelineの数字だけを対象ポストへ差分反映するよう修正した。ブックマークは履歴カラムだけを更新し、投稿・返信・引用だけを全体更新トリガーとして維持する。
- X Web版と同じ初期表示にするため、タイムライン応答の`grok_translated_post_with_availability`を保持して対象言語が一致する事前翻訳を通信なしで表示し、事前翻訳がないポストだけリアルタイム翻訳するよう修正した。あわせて、全ポスト取得経路でNyTweetDeckのUI言語をXへ渡すよう統一した。
- 起動直後の複数カラム取得を安定させるため、タイムライン・トレンド・X翻訳のブラウザー通信を共通15秒上限へ統一し、Live Pipelineの欠落フラグを`false`へ正規化するとともに、切断済みFirefox SSEを有限時間で安全に破棄するよう修正した。
- X Web版と操作結果を一致させるため、いいね・リポスト・ブックマークのMutationを現行`x.com/i/api/graphql`へ統一し、POST bodyの`queryId`と、現行Webアセットから動的生成する`x-client-transaction-id`を付与した。404または誤った制限応答では署名材料を1回だけ再取得し、失敗時はポスト欄に秘匿済みの理由を表示するよう修正した。
- 返信ポストを通常ポストと区別できるよう、親ポストIDと返信先@ユーザー名を正規化し、アプリ内の親ポスト詳細へ移動できる返信文脈をカードへ追加した。
- 広告ポストを表示しないため、全タイムライン要求の`includePromotedContent`を`false`へ統一し、promoted metadataを持つ応答サブツリーも正規化前に除外するよう修正した。
- リスト候補を現行`TimelineTwitterList.list`／`id_str`構造から復元し、`ListSearchTimeline`へ廃止済み`querySource`を送らず現行の`rawQuery`＋`count=20`へ揃えることで、自分・おすすめ・検索結果が空になる問題を修正した。
- Firefoxでトレンド取得がサーバー到達前にstallして「読み込み中」のまま残る場合に備え、ブラウザー側通信を15秒で中断し、エラー表示から再試行できるよう修正した。
- 現行`ExplorePage`の`TimelineTrend.trend_url`／`trend_metadata`へパーサーを更新し、不要な旧`url`互換を削除するとともに、通信成功時も全トレンドを破棄して「トレンドなし」になる問題を修正した。
- 自動翻訳をXネイティブ提供機能へ限定するため、Feature Switch無効時のGoogle翻訳フォールバックを削除し、X公式Stratoへ常に`translationSource=X`を指定するよう修正した。
- 検索カラムが現行X Web APIで404になる問題を解消するため、`SearchTimeline`を旧GET形式ではなく、変数・Feature Switch・Field ToggleをJSONへ含めるPOST形式で送信するよう修正した。
- 履歴カラムでも投稿者を識別できるように、Bookmarks応答の入れ子および単数形のユーザー参照を展開し、アイコン、名前、@ユーザー名が欠落する問題を修正した。
- 最新ポストが上に来る要件と逆のAPI応答順を保持していたため、正規化した投稿時刻で降順に並べ、相対投稿時刻を選択言語で毎分更新するよう修正した。
- 端末固有証明を要求する旧モバイル経路を廃止し、ログインと認証済み通信をX公式Webセッションへ一本化した。
- 新JetfuelログインでSMS・アプリ本人確認が進まない場合を避けるため、現在も公式配信されているResponsive Webログインへ導線を変更した。
- ログイン完了時にChromeのバックグラウンドプロセスをウィンドウと誤認して取り込みを拒否していたため、ログイン中の同じ専用ChromeからWebセッションを直接保存するよう修正した。
- ログイン済みWebセッションを旧v1.1アカウント設定APIで再確認して拒否していたため、X自身が発行した必須HttpOnly Cookieとログイン済み画面のアカウント情報を検証して保存するよう修正した。
- 現行Webユーザー応答の`core`・`avatar`・`verification`を読めず作者が空欄になっていたため、ID・名前・ユーザー名・画像・認証表示を新旧スキーマから正規化するよう修正した。
- 検索・ユーザー・ポスト詳細・リスト・投稿操作が旧operation IDと変数で失敗していたため、X公式Webの現行GraphQL定義へ更新し、部分データ付きの非致命的エラーを表示可能な範囲で受理するよう修正した。
- 通知が種類を失って外部Webへ遷移していたため、現行`NotificationsTimeline`へ切り替え、いいね・リポスト・返信・フォロー等を分類して対象ポストをアプリ内詳細で開くよう修正した。
- DM初期読込へ履歴ページ用URLを使って400になっていたため、初回は現行`dm/inbox_initial_state`、ページ送りは`dm/inbox_timeline/trusted`を使い分けるよう修正した。
- トレンド選択でX Webへ移動していたため、NyTweetDeck内に該当語の検索カラムを追加するよう修正した。
- コミュニティノート通知で48文字の定型文を別モーダルへ複製するだけだった問題を解消するため、全通知の`notification_url`からノートIDを抽出し、対象ポスト・完全なノート本文・出典リンクをWeb版準拠の統合詳細として表示するよう修正した。
- ブラウザUI検証内の失敗を成功扱いしないよう、PowerShellラッパーがBunの非0終了コードを検出してCIへ伝播するよう修正した。
- 起動方法や配布先を変えると保存済みアカウントが見えなくなる問題を解消するため、アカウントとログイン用一時プロファイルの既定保存先をOS標準のユーザー別領域へ固定し、旧作業ディレクトリと`target/.local`から最新の有効データを自動移行するよう修正した。

### Removed

- 再起動や自動検証のたびに人手を要求しないよう、手動パスフレーズ、作成・解除・ロックAPI、ロック状態による通信停止、設定画面の資格情報ロック機能を削除した。

### Security

- 悪意あるWebページからローカルAPI操作やレート消費を誘発されないよう、APIのloopback Host検証、Origin同一性、Fetch Metadata検証とcross-origin分離ヘッダーを追加した。
- API定義更新を利用した任意URL取得を防ぐため、更新元をHTTPSの`x.com`と`abs.twimg.com`に限定し、資産サイズ・operation形式・必須定義の完全性を検証するようにした。
- 手動解除をなくして自動起動を成立させる設計判断として、所有者の明示的な許可に基づきX WebセッションをOS標準のユーザー別データ領域へ平文保存する。共有PCでは使用しない警告をREADMEへ記載し、API・ログ・`localStorage`へ秘密情報を返さない境界を維持する。

### Changed

- 初めて使う人が起動から日常操作まで順に確認できるよう、READMEを配布ZIPの起動、Xログイン、カラム追加、基本操作、設定、トラブル対応を先に案内する構成へ再編した。
- Java 17・21・25を同一JARで正式対応するため、コンパイル対象をJava 17へ変更し、仮想スレッド等のJava 21専用APIをJava 17互換のデーモンスレッド実装へ置き換えた。
- リポスト元を通常ポストと同じ情報量で読めるように、外側のリポスト者は薄灰色の小さなアイコン付き表示だけにし、作者・本文・メディア・数字・操作を元ポストへ統一した。
- 引用元を外部Webへ頼らず文脈ごと確認できるように、作者・本文・時刻・メディアを境界付きのWeb準拠カードで表示し、選択時はアプリ内詳細へ遷移するよう変更した。
- 通知とトレンドを他のカラムから浮かせず連続して読めるように、投稿カードと同じ背景・境界・余白・文字階層・ホバー／フォーカス表現へ統一した。
- X上の反応済み状態をひと目で区別できるよう、いいね済みは塗りつぶしたピンクのハート、リポスト済みは緑で初期表示し、操作成功後も同じ色へ即時更新するよう変更した。
- 一般的な開発サーバーとのポート競合を減らすため、専用ローカルアドレスの既定ポートを18080へ変更した。
